# Scenery Console
Vite React Astryx + StyleX dashboard served by `scenery up` at `/console/`.
Run locally: `bun run dev`.
Build this app: `bun run build`.
Build embedded dashboard: from the repo root, `./scripts/build-dashboard-ui-embed.sh`.
