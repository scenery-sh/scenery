{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://onlava.com/schemas/onlava.check.result.v1.schema.json",
  "title": "onlava.check.result.v1",
  "description": "Schema for `onlava check --json` responses.",
  "type": "object",
  "required": ["schema_version", "ok", "app", "diagnostics"],
  "properties": {
    "schema_version": { "const": "onlava.check.result.v1" },
    "ok": { "type": "boolean" },
    "app": {
      "type": "object",
      "required": ["name", "root", "config_path"],
      "properties": {
        "name": { "type": "string" },
        "id": { "type": "string" },
        "root": { "type": "string" },
        "config_path": { "type": "string" },
        "module_path": { "type": "string" }
      },
      "additionalProperties": false
    },
    "diagnostics": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["stage", "severity", "message"],
        "properties": {
          "stage": { "type": "string" },
          "severity": { "type": "string", "enum": ["error", "warning", "info"] },
          "file": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "column": { "type": "integer", "minimum": 1 },
          "message": { "type": "string" },
          "suggested_action": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
