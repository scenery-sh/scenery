{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://scenery.sh/schemas/scenery.go-build-input-manifest.schema.json",
  "title": "Scenery Go Build Input Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "schema_revision", "spec_revision", "producer", "target", "entries", "digest"],
  "properties": {
    "kind": { "const": "scenery.go-build-input-manifest" },
    "schema_revision": { "$ref": "#/$defs/digest" },
    "spec_revision": { "$ref": "#/$defs/digest" },
    "producer": { "type": "object" },
    "target": { "type": "string", "minLength": 1 },
    "entries": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["identity", "digest"],
        "properties": {
          "identity": { "type": "string", "minLength": 1 },
          "digest": { "$ref": "#/$defs/digest" }
        }
      }
    },
    "digest": { "$ref": "#/$defs/digest" }
  },
  "$defs": {
    "digest": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    }
  }
}
