{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://onlava.com/schemas/onlava.docs.index.v1.schema.json",
  "title": "onlava.docs.index.v1",
  "type": "object",
  "required": ["schema_version", "generated_at", "owner_default", "freshness_policy", "documents", "plans", "tech_debt"],
  "properties": {
    "schema_version": { "const": "onlava.docs.index.v1" },
    "generated_at": { "type": "string" },
    "owner_default": { "type": "string" },
    "freshness_policy": {
      "type": "object",
      "required": ["default_review_days", "quality_grades", "freshness_states"],
      "properties": {
        "default_review_days": { "type": "integer" },
        "quality_grades": {
          "type": "array",
          "items": { "enum": ["A", "B", "C", "D"] }
        },
        "freshness_states": {
          "type": "array",
          "items": { "enum": ["current", "review_due", "stale"] }
        }
      },
      "additionalProperties": false
    },
    "documents": {
      "type": "array",
      "items": { "$ref": "#/$defs/document" }
    },
    "plans": {
      "type": "object",
      "required": ["active", "completed"],
      "properties": {
        "active": { "type": "string" },
        "completed": { "type": "string" }
      },
      "additionalProperties": false
    },
    "tech_debt": { "type": "string" }
  },
  "additionalProperties": false,
  "$defs": {
    "document": {
      "type": "object",
      "required": ["path", "title", "owner", "status", "quality", "freshness", "last_reviewed", "review_after", "summary", "tags"],
      "properties": {
        "path": { "type": "string" },
        "title": { "type": "string" },
        "owner": { "type": "string" },
        "status": { "enum": ["active", "reference", "completed", "deprecated"] },
        "quality": { "enum": ["A", "B", "C", "D"] },
        "freshness": { "enum": ["current", "review_due", "stale"] },
        "last_reviewed": { "type": "string" },
        "review_after": { "type": "string" },
        "summary": { "type": "string" },
        "tags": {
          "type": "array",
          "items": { "type": "string" }
        },
        "schema_refs": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "additionalProperties": false
    }
  }
}
